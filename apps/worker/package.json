{
  "name": "@receipt-report/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@receipt-report/api": "workspace:*",
    "@receipt-report/config": "workspace:*",
    "@receipt-report/contracts": "workspace:*",
    "@receipt-report/database": "workspace:*",
    "@receipt-report/logging": "workspace:*",
    "@receipt-report/receipt-ai": "workspace:*",
    "file-type": "21.3.0",
    "imapflow": "1.0.195",
    "sharp": "0.34.5"
  }
}
